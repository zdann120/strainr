<div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom box-shadow">
      <h5 class="my-0 mr-md-auto font-weight-normal">Strainr</h5>
      <nav class="my-2 my-md-0 mr-md-3">
        <%= link_to 'Home', root_path, class: 'p-2 text-dark' %>
        <%= link_to 'Strains', strains_path, class: 'p-2 text-dark' %>
      </nav>
      <% if current_user %>
        <%= link_to 'Sign out', destroy_user_session_path, method: :delete,
          class: 'btn btn-outline-danger' %>
      <% else %>
        <%= link_to 'Login', new_user_session_path, class: 'btn btn-outline-success' %>
      <% end %>
    </div>
